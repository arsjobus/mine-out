name: Build and Release

on:
  push:
    tags:
      - 'v*'  # e.g., v1.0.0
  workflow_dispatch:

jobs:
  build-macos:
    name: Build and Release on macOS
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Homebrew dependencies
        run: |
          brew update
          brew install sfml cmake

      - name: Create build directory
        run: mkdir -p build

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES="arm64"

      - name: Run macOS app bundle script
        run: ./make_macos_app.sh
        shell: bash

      - name: Archive .app bundle
        run: |
          cd build
          zip -r MineOut-macos.zip MineOut.app

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: mineout-macos
          path: build/MineOut-macos.zip
          retention-days: 7

  build-windows:
    name: Build and Release on Windows
    runs-on: windows-latest
    env:
      VCPKG_DEFAULT_TRIPLET: x64-windows

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install CMake and Ninja
        uses: lukka/get-cmake@latest

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: ${{ github.workspace }}/vcpkg

      - name: Install SFML dependencies
        run: vcpkg install sfml --triplet x64-windows
        shell: pwsh

      - name: Configure CMake for Windows
        run: |
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_TOOLCHAIN_FILE="${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET=x64-windows
        shell: pwsh

      - name: Build Windows executable
        run: cmake --build build --config Release
        shell: pwsh

      - name: Prepare Windows release bundle
        run: |
          $bundle = Join-Path $PWD "build/windows-dist"
          New-Item -ItemType Directory -Force -Path $bundle | Out-Null
          Copy-Item build/game.exe $bundle
          Copy-Item -Recurse data $bundle
          Copy-Item -Recurse fonts $bundle
          Copy-Item -Recurse images $bundle
          Copy-Item -Recurse music $bundle
          Copy-Item -Recurse sfx $bundle

          $dllDir = Join-Path $PWD "vcpkg/installed/x64-windows/bin"
          Get-ChildItem $dllDir | Where-Object { $_.Name -like "sfml*.dll" -or $_.Name -like "openal*.dll" } | ForEach-Object {
            Copy-Item $_.FullName $bundle
          }

          Compress-Archive -Path (Join-Path $bundle "*") -DestinationPath "MineOut-windows.zip" -Force
        shell: pwsh

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: mineout-windows
          path: MineOut-windows.zip
          retention-days: 7

  release:
    name: Publish GitHub Release
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/mineout-macos/MineOut-macos.zip
            dist/mineout-windows/MineOut-windows.zip
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
